{
  "name": "biofabric-web",
  "private": true,
  "version": "0.1.0",
  "description": "BioFabric web frontend — interactive network visualization using WebGL2 + WASM",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "wasm:build": "wasm-pack build ../crates/wasm --target web --out-dir ../../web/src/wasm/pkg"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4",
    "vite": "^6.0"
  }
}
